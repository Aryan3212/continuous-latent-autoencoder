# CLAUDE.md

**No Python on this machine.** Do not run `python`, `python3`, or any script that requires a Python interpreter. Do not attempt smoke tests, import checks, or any Python invocation. Verify correctness by reading the code only.

See `agents.md` for additional agent instructions and `CODEBASE.md` for codebase orientation.
